name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  NODE_VERSION: 20

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Extract version
        id: ver
        shell: bash
        run: echo "v=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Write build info
        shell: bash
        run: |
          SHA=$(git rev-parse --short HEAD)
          DATE=$(date -u +"%Y-%m-%d %H:%M UTC")
          printf 'window.__BLFP_BUILD__ = { sha: "%s", builtAt: "%s" };\n' "$SHA" "$DATE" > renderer/build-info.js
          cat renderer/build-info.js

      - name: Install dependencies
        run: |
          npm ci
          cd uninstaller && npm ci && cd ..
          cd installer && npm ci && cd ..

      - name: Strip dev-only files
        shell: bash
        run: |
          # 布局调试器只用于开发期微调，正式发布版必须剔除
          rm -f renderer/layout-tuner.js renderer/layout-tuner.css
          sed -i '/layout-tuner/d' renderer/index.html
          if grep -rn 'layout-tuner' renderer/ ; then
            echo '::error::布局调试器未被完全剔除，发布版不应包含它'
            exit 1
          fi
          echo '已剔除布局调试器'

      - name: Build uninstaller
        run: cd uninstaller && npm run dist -- --publish never

      - name: Build client directory
        run: npm run dist -- --publish never

      - name: Verify client build
        shell: powershell
        run: |
          $ver = "${{ steps.ver.outputs.v }}"
          $root = Join-Path "build_v$ver" "win-unpacked"
          $required = @(
            (Join-Path $root "BLFP.exe"),
            (Join-Path $root "resources\bin\easytier-core.exe"),
            (Join-Path $root "resources\bin\easytier-cli.exe"),
            (Join-Path $root "resources\bin\frpc.exe"),
            (Join-Path $root "resources\bin\wintun.dll"),
            (Join-Path $root "resources\app\renderer\index.html"),
            (Join-Path $root "resources\app\renderer\app.js"),
            (Join-Path $root "resources\app\renderer\style.css"),
            (Join-Path $root "resources\app\main.js"),
            (Join-Path $root "resources\app\src\easytier-manager.js")
          )
          $missing = @()
          foreach ($f in $required) {
            if (-not (Test-Path $f)) { $missing += $f }
          }
          if ($missing.Count -gt 0) {
            Write-Host "FAILED - client build is missing:"
            $missing | ForEach-Object { Write-Host "   $_" }
            throw "client build incomplete"
          }
          Write-Host "OK - client build verified (exe + bin runtime + renderer + src)"
          $binSum = (Get-ChildItem (Join-Path $root "resources\bin") | Measure-Object -Property Length -Sum).Sum
          Write-Host ("   bin total: " + [math]::Round($binSum/1MB,1) + " MB")

      - name: Assemble payload
        shell: powershell
        run: |
          $ver = "${{ steps.ver.outputs.v }}"
          $clientOutput = Join-Path "build_v$ver" "win-unpacked"
          $payloadDir = Join-Path "installer" "payload"
          if (Test-Path $payloadDir) { Remove-Item -Recurse -Force $payloadDir }
          New-Item -ItemType Directory -Path $payloadDir -Force | Out-Null
          Compress-Archive -Path "$clientOutput/*" -DestinationPath (Join-Path $payloadDir "payload.zip") -Force
          $uninstallerExe = Join-Path (Join-Path "uninstaller" "out_v$ver") "BLFP-Uninstall.exe"
          if (-not (Test-Path $uninstallerExe)) { throw "uninstaller exe not found: $uninstallerExe" }
          Copy-Item -Path $uninstallerExe -Destination (Join-Path $payloadDir "BLFP-Uninstaller.exe") -Force
          $zip = Join-Path $payloadDir "payload.zip"
          if (-not (Test-Path $zip)) { throw "payload.zip was not created" }
          $zipMB = [math]::Round((Get-Item $zip).Length / 1MB, 1)
          if ($zipMB -lt 80) { throw "payload.zip too small: $zipMB MB" }
          Write-Host ("OK - payload assembled: payload.zip = " + $zipMB + " MB")
          Get-ChildItem $payloadDir | ForEach-Object { Write-Host ("   " + $_.Name + "  " + [math]::Round($_.Length/1MB,2) + " MB") }

      - name: Build installer
        run: cd installer && npm run dist -- --publish never

      - name: Verify installer
        shell: powershell
        run: |
          $ver = "${{ steps.ver.outputs.v }}"
          $exe = "installer\out_v$ver-final\BLFP-Setup-v$ver.exe"
          if (-not (Test-Path $exe)) { throw "installer not found: $exe" }
          $mb = [math]::Round((Get-Item $exe).Length / 1MB, 1)
          Write-Host ("installer size: " + $mb + " MB")
          if ($mb -lt 150) { throw "installer too small ($mb MB) - payload probably missing" }
          Write-Host "OK - installer verified"

      - name: Upload release
        uses: softprops/action-gh-release@v3
        with:
          files: installer/out_v${{ steps.ver.outputs.v }}-final/BLFP-Setup-v${{ steps.ver.outputs.v }}.exe
          # tag 名含 '-'（如 v2.3.1-pre）一律标为预发布，避免被 /releases/latest 返回而推送给正式用户
          prerelease: ${{ contains(steps.ver.outputs.v, '-') }}
          body: |
            ## BLFP v${{ steps.ver.outputs.v }}

            ### 🐛 修复
            - 安装程序：等待旧进程退出超时导致 EBUSY「资源被占用」安装失败
            - 安装程序：安装完成后无法正常唤醒客户端
            - 修复搜索好友报错 `f is not defined`
            - 修复「收集诊断信息」报错 `showModal is not defined`
            - 运行日志刷屏：HTTP 安全警告改为每个来源只提示一次
            - 公开房间列表不再每 30 秒刷一条日志
            - 退出登录偶发卡死：逐步容错，确保会话清理完成
            - 弹窗背景过透、退出确认按钮点不到
            - 通知消失时增加滑出动画，不再突然消失
            - 开房不再短暂停留「创建房间」页面：进入即显示「正在创建」状态

            ### 🎨 界面
            - 侧边栏、开关控件、EasyTier 测速区、关闭房间按钮等 8 处间距微调
            - EasyTier 测速结果正式排版：节点名靠左、延迟靠右，正常绿色、不可达红色

            ### ⚙️ 其他
            - CI Actions 升级至最新大版本（checkout v7 / setup-node v7 / gh-release v3）

            ### 构建信息
            - 构建 commit：`${{ github.sha }}`
            - 详细构建时间见包内 `renderer/build-info.js`
